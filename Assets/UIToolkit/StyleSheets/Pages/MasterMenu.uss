.master-menu {
    background-color: var(--middle-gray);
}

.title {
    font-size: 80px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    color: black;
}

.button-container {
    align-items: center;
    margin-top: 100px;
    margin-bottom: 100px;
}